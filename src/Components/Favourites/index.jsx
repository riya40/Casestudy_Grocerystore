import React from "react";
import { Grid } from "@mui/material";

const Favourites = ()=>{
    return(
        <Grid Container>
            <h1>
                this is home page
            </h1>
        </Grid>
    );
};

export default Favourites;